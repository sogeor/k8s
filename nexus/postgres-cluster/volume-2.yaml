apiVersion: v1
kind: PersistentVolume
metadata:
  name: nexus-postgres-cluster-volume-2
  labels:
    app.kubernetes.io/name: nexus-postgres-cluster-volume
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 16Gi
  local:
    path: /root/data/nexus/postgres-cluster/volume-2
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - "maxwell"
  persistentVolumeReclaimPolicy: Retain
  storageClassName: native-storage
