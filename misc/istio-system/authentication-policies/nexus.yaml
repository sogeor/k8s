apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: nexus
  namespace: istio-system
spec:
  selector:
    matchLabels:
      istio: ingressgateway
  action: ALLOW
  rules:
    - to:
        - operation:
            hosts:
              - "nexus.sogeor.com"
              - "nexus.sogeor.com:*"
