apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: api
  namespace: istio-system
spec:
  selector:
    matchLabels:
      istio: ingressgateway
  action: ALLOW
  rules:
    - from:
        - source:
            requestPrincipals:
              - "https://sso.sogeor.com/realms/master/*"
              - "https://sso.sogeor.com/realms/system/*"
              - "https://sso.sogeor.com/realms/public/*"
      to:
        - operation:
            hosts:
              - "api.sogeor.com"
              - "api.sogeor.com:*"
            notPaths:
              - "/robots.txt"
              - "/oauth2"
              - "/oauth2/{**}"
              - "/v1/docs-server"
              - "/v1/docs-server/{**}"
    - to:
        - operation:
            hosts:
              - "api.sogeor.com"
              - "api.sogeor.com:*"
            paths:
              - "/robots.txt"
              - "/oauth2"
              - "/oauth2/{**}"
              - "/v1/docs-server"
              - "/v1/docs-server/{**}"
