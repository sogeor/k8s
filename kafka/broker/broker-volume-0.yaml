apiVersion: v1
kind: PersistentVolume
metadata:
  name: kafka-broker-volume-0
  labels:
    app.kubernetes.io/name: kafka-broker-volume
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 16Gi
  local:
    path: /root/data/kafka/broker/volume-0
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - "maxwell"
  persistentVolumeReclaimPolicy: Retain
  storageClassName: native-storage
