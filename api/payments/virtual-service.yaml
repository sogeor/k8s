apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: payments
  namespace: api
spec:
  hosts:
    - "api.sogeor.com"
  gateways:
    - "istio-system/master"
  http:
    - match:
        - uri:
            prefix: /v1/payments
          port: 9090
      route:
        - destination:
            port:
              number: 9090
            host: payments.api.svc.cluster.local
            subset: v1
    - match:
        - uri:
            prefix: /v1/payments
      route:
        - destination:
            port:
              number: 8080
            host: payments.api.svc.cluster.local
            subset: v1
  exportTo:
    - "istio-system"
