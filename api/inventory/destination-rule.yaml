apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: inventory
  namespace: api
spec:
  host: inventory.api.svc.cluster.local
  subsets:
    - name: v1
      labels:
        app.kubernetes.io/version: v1
