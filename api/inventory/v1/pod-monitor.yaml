apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: inventory-v1
  namespace: api
spec:
  podMetricsEndpoints:
    - interval: 15s
      scrapeTimeout: 10s
      path: /v1/inventory/actuator/prometheus
      port: http-management
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory-service
      app.kubernetes.io/version: v1
