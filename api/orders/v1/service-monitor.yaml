apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: orders-v1
  namespace: api
spec:
  endpoints:
    - interval: 15s
      scrapeTimeout: 10s
      path: /v1/orders/actuator/prometheus
      port: http-management
  selector:
    matchLabels:
      app.kubernetes.io/name: orders-service
      app.kubernetes.io/version: v1
